import type { Metadata } from 'next';
import { ProsePage } from '@/components/ProsePage';
export const metadata: Metadata = { title: 'Methodology', description: 'How IQTestReal selects questions and calculates Estimated IQ.', alternates: { canonical: '/methodology' } };
export default function MethodologyPage() { return <ProsePage kicker="Methodology / Transparent by design" title="Know how the estimate is made." intro="IQTestReal uses a balanced, randomized practice set and a visible scoring model."><h2>Five categories</h2><p>Every attempt includes three questions from logic, pattern, numerical, spatial, and analogy reasoning. The question bank is larger than a single test and avoids duplicate IDs within an attempt.</p><h2>Randomization</h2><p>Questions are sampled independently within each category. Answer choices are shuffled after selection, while the correct answer stays attached to its text.</p><h2>Scoring</h2><p>Accuracy is converted with <code>100 + (accuracy − 50) × 0.6</code>, bounded to 70–130. Percentile bands and classifications are approximate references, not population norms.</p><h2>Limitations</h2><p>Three questions per category cannot provide precise measurement. Language, attention, fatigue, device, familiarity, and environment affect results.</p></ProsePage>; }
